// Merge resolver module — the flagship 3-panel IntelliJ-style conflict resolver
// (LEFT / CENTER result / RIGHT), hunk navigation, accept/reject, optional
// AI-assisted suggestion (PRD §4.5).
//
// TODO (Phase 2): diff engine, hunk navigator, 3-panel UI.

export const MERGE_RESOLVER_MODULE = "merge-resolver" as const;
